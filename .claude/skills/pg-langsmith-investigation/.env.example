# Copie este arquivo para .env.local (gitignorado) e cole as chaves reais.
# As chaves originais vieram embutidas nos scripts do zip da skill —
# foram removidas do codigo porque ESTE REPOSITORIO E PUBLICO.
#
#   copy .env.example .env.local     (PowerShell/cmd)
#   cp   .env.example .env.local     (bash)
#
# Ambas sao read-only. Se vazarem, rotacione no LangSmith / Metabase.

LANGSMITH_API_KEY=lsv2_pt_cole_aqui
METABASE_API_KEY=mb_cole_aqui
